/**
 * Canonical production host for SEO (metadata, sitemap, JSON-LD).
 * Override with NEXT_PUBLIC_SERVER_URL in env (e.g. local dev).
 */
export const PRODUCTION_SITE_URL = 'https://mixiekadai.lk'

export const SITE_NAME = 'Mixie Kadai'
